<!--
The markup in NativeScript apps contains a series of user interface components, each
of which NativeScript renders with a platform-specific iOS or Android native control.
You can find a full list of user interface components you can use in your app at
https://docs.nativescript.org/ui/components.
-->
<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="onNavigatingTo" class="page">
    <!--
    The ActionBar is the NativeScript common abstraction over the Android ActionBar and iOS NavigationBar.
    http://docs.nativescript.org/ui/action-bar
    -->
    <Page.actionBar>
        <ActionBar title="{{ carModel.name }}" icon="" class="action-bar">
            <NavigationButton tap="onGoBack" android.systemIcon="ic_menu_back"/>
        </ActionBar>
    </Page.actionBar>

    <GridLayout>
        <ScrollView>
            <StackLayout>
                <Image src="{{ carModel.imageUrl }}" stretch="aspectFit"/>

                <StackLayout>
                    <Label text="Price"/>
                    <Label>
                        <FormattedString>
                            <Span text="€" fontAttributes="Bold"></Span>
                            <Span text="{{ carModel.price }}" fontAttributes="Bold"></Span>
                            <Span text="/day" fontAttributes="Bold"></Span>
                        </FormattedString>
                    </Label>
                </StackLayout>

                <StackLayout>
                    <Label text="Class"/>
                    <Label text="{{ carModel.class }}"/>
                </StackLayout>

                <StackLayout>
                    <Label text="Transmission"/>
                    <Label text="{{ carModel.transmission }}"/>
                </StackLayout>

                <StackLayout>
                    <Label text="Doors"/>
                    <Label text="{{ carModel.doors }}"/>
                </StackLayout>

                <StackLayout>
                    <Label text="Luggage"/>
                    <Label text="{{ carModel.luggage }}"/>
                </StackLayout>

                <StackLayout>
                    <Label text="Seats"/>
                    <Label text="{{ carModel.seats }}"/>
                </StackLayout>

                <StackLayout>
                    <Label text="Other information"/>
                    <Label text="{{ carModel.description }}" textWrap="true"/>
                </StackLayout>
            </StackLayout>
        </ScrollView>
    </GridLayout>
</Page>
